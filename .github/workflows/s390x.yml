name: Build Detect It Easy s390x

on:
  workflow_dispatch:
  push:
    branches:
      - "*"

env:
  SRC_PATH: DIE-engine

jobs:
  build-s390x:

    name: Build s390x (CMake)
    runs-on: ubuntu-24.04

    steps:

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3


      - name: Clone DIE-engine
        run: |
          git clone https://github.com/horsicq/DIE-engine.git --recursive ${{ env.SRC_PATH }}


      - name: Build s390x
        run: |
          printf '%s\n' \
            'set -ex' \
            'apt-get update' \
            'DEBIAN_FRONTEND=noninteractive apt-get install -y \
              build-essential \
              cmake \
              ninja-build \
              git \
              qtbase5-dev \
              qttools5-dev \
              libqt5svg5-dev \
              qt5-qmake \
              tar \
              gzip' \
            'echo Architecture:' \
            'uname -m' \
            'cd /src/DIE-engine' \
            'mkdir -p build' \
            'cd build' \
            'cmake .. -DCMAKE_BUILD_TYPE=Release' \
            'cmake --build . -j$(nproc)' \
            'mkdir -p /src/package' \
            'cp -a ./* /src/package/ 2>/dev/null || true' \
            'cd /src' \
            'tar -czf detect-it-easy-s390x.tar.gz package' \
          | docker run --rm -i \
              --platform linux/s390x \
              -v "${PWD}:/src" \
              -w /src \
              s390x/ubuntu:24.04 \
              bash


      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: detect-it-easy-s390x
          path: detect-it-easy-s390x.tar.gz


      - name: Upload Release
        if: github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v3
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        with:
          tag_name: Beta
          draft: false
          prerelease: true
          files: |
            detect-it-easy-s390x.tar.gz