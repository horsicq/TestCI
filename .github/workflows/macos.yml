name: macOS CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-osx:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Qt via Homebrew
        run: |
          brew update
          brew install qt@5

      - name: Clone DIE-engine
        run: |
          git clone https://github.com/horsicq/DIE-engine.git --recursive

      - name: Patch CMake Scope Errors
        run: |
          sed -i '' '/macdeployqt\.cmake/s/^/#/' DIE-engine/src/lite/CMakeLists.txt || true
          sed -i '' '/macdeployqt\.cmake/s/^/#/' DIE-engine/src/console/CMakeLists.txt || true

      - name: Build DiE macOS
        run: |
          export QT_DIR="$(brew --prefix qt@5)"
          export PATH="$QT_DIR/bin:$PATH"
          export CMAKE_PREFIX_PATH="$QT_DIR"
          export QT_PREFIX_PATH="$QT_DIR"
          
          export CMAKE_OSX_ARCHITECTURES="arm64"
          
          export CFLAGS="-Wno-error=implicit-function-declaration -Wno-error=incompatible-pointer-types -Wno-error=implicit-int -Wno-implicit-function-declaration"
          export CXXFLAGS="$CFLAGS"
          
          cd DIE-engine
          chmod a+x build_mac.sh
          ./build_mac.sh

      - name: Upload macOS Release
        if: github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: macOS-Beta
          draft: false
          prerelease: true
          files: |
            DIE-engine/release/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}